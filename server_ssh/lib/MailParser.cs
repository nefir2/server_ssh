using System.Collections.Generic;
using System.IO;
namespace server_ssh.lib
{
	static public class MailParser
	{
		//Dictionary
		public static void Parse(string pathOfMaildir)
		{
			using (FileStream sf = new FileStream(pathOfMaildir, FileMode.Open))
			{

			}
		}
	}
}
